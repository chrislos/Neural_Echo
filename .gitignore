# Audio gehoert nicht ins Repo: die Aufnahmen in neural_echo/static sind
# zusammen ~850 MB und werden separat auf den Rechner kopiert.
*.mp3
*.wav

# Ableton legt neben jeder Audiodatei eine Analyse-Datei ab.
*.asd

.DS_Store
**/.DS_Store

# Alles Weitere (node_modules, dist, Logs, standort.conf, Xcode-Kram) steht in
# neural_echo/.gitignore, direkt neben den Ordnern, um die es geht.
